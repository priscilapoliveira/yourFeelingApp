import { useState, useEffect } from 'react'

import api from '../services/api'

export default (emojiApiData) => {
  const [data, setData] = useState([])
  const [error, setError] = useState(null)

  // useEffect(()=>{

  // },[])
}
